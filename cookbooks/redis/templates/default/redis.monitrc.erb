check process redis2-<%= @profile %>
with pidfile <%= @pidfile %>
  start program = "/usr/local/bin/redis-server <%= @configfile %>"
  stop program = "/usr/local/bin/redis-cli -p <%= @port %> shutdown"
  group redis-util
